import * as types from "./types";

export function fetchReflections() {
    return (dispatch, getState) => {
        const params = [
            
        ]
    }
}

export function addReflection() {
    return {
        type: types.ADD_REFLECTION
    }
}