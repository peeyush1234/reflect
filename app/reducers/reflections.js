import createReducer from "../lib/createReducer";
import * as types from "../actions/types";

export const fetchedReflections = createReducer({}, {

});

export const currentReflection = createReducer({}, {

});